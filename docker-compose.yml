services:
  pixelebbe:
    build: .
    ports:
      - "8000:5001"
    environment:
      - SIP_SERVER=sip.micropoc.de
      - SIP_USER=9379
      - SIP_PASS=89783690294e
      - HOTLINE_NUMBER=74950
    volumes:
      - ./drawing.json:/app/drawing.json
      - whisper_cache:/root/.cache   # persist downloaded Whisper model across restarts
    restart: unless-stopped

volumes:
  whisper_cache:
